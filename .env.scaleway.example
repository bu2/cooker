# Copy to .env.scaleway and fill in values

# Instance (Ubuntu Noble) connection
SCW_SSH_HOST=1.2.3.4
SCW_SSH_USER=ubuntu
SCW_SSH_PORT=22
# SCW_SSH_KEY=~/.ssh/id_rsa

# Object Storage
SCW_S3_BUCKET=your-bucket-name
SCW_S3_REGION=nl-ams
# SCW_S3_ENDPOINT=https://s3.nl-ams.scw.cloud

# Frontend build: where the app calls the API (your instance URL)
PUBLIC_API_BASE_URL=https://api.your-domain

# Public base URL where images will be served (Object Storage/CDN)
# This should be the website origin hosting your static site.
# Example: PUBLIC_IMAGES_BASE_URL=https://www.your-domain
PUBLIC_IMAGES_BASE_URL=

# Optional: Email for Let's Encrypt/ACME (Caddy). Recommended for HTTPS.
# Example: ACME_EMAIL=you@example.com
ACME_EMAIL=

# S3 credentials (from Scaleway IAM for Object Storage)
AWS_ACCESS_KEY_ID=AKIAXXXXX
AWS_SECRET_ACCESS_KEY=xxxxxxxx

# Optional
# If true, uploads data/images to Object Storage under s3://$SCW_S3_BUCKET/images
SYNC_IMAGES=false
DOCKER_IMAGE_TAG=cooker:latest
REMOTE_APP_DIR=/opt/cooker
