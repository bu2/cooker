# Public API base URL for the frontend (Vite only reads vars prefixed with VITE_)
# When set, the app will call `${VITE_API_BASE_URL}/...` for API routes and images.
# Example values:
#   VITE_API_BASE_URL=http://localhost:8000
#   VITE_API_BASE_URL=https://api.your-domain.com
# If not set:
#   - In dev, the app uses Vite's proxy at `/api` (see vite.config.ts)
#   - In production, the app uses the same origin (behind your reverse proxy)
VITE_API_BASE_URL=
